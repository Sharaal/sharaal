<div class="row marketing">
  <div class="col-md-12">
    <h4 class="text-center">
      <a href="mailto:{{ contacts.email }}">{{ contacts.email }}</a>
    </h4>
  </div>
</div>
<div class="row marketing">
  {% set columns = 12 / contacts.links.length %}
  {% for link in contacts.links %}
    {% include "./contacts/contact.html.twig" with link %}
  {% endfor %}
</div>
